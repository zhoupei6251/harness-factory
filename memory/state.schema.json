{
  "$comment": "运行时状态 schema 模板——2026-07-02 后已停更；保留为契约参考。运行时实例更适合放在 skill cache（如 skills/harness-health/cache/），不进仓库本体。",
  "version": "1.0.0",
  "fields": {
    "version": "string (semver)",
    "last_updated": "ISO 8601 timestamp",
    "active_domain": "string|null (code|novel|news|null)",
    "active_phase": "string (idle|brainstorm|plan|implement|review|done)",
    "active_platforms": "string[] (claude|trae|codex|workbuddy)",
    "active_wus": "WorkUnit[] (当前活跃 work unit 列表)",
    "sessions": "Session[] (会话元数据)",
    "learning": {
      "last_extract": "ISO 8601 timestamp|null",
      "total_patterns": "number",
      "active_patterns": "Pattern[]",
      "by_type": "{[type]: number} 计数器"
    }
  }
}
