# engine/START.md — 一页纸母版

> `compiler/render.ts` 读本文件 + `engine/gates.md` + `engine/routes/<line>.md`
> + `engine/platforms/<plat>.md` + `.workspaces/<proj>/{config.md,facts/profile.md}`
> → 渲染各平台一页纸。**产物硬预算 ≤120 行**（`compiler/lint.ts` 执法，超了 CI 红）。
> `{{slot}}` 由 render 填充。不允许 `{{#if}}`——条件性只允许出现在「档」与「路线」两个维度，
> 第三个维度出现分支就说明引擎在往模板引擎退化。

## 生成头

    GENERATED BY harness-factory — DO NOT EDIT
    engine:   {{engine_path}} @ {{engine_commit}}
    workspace: {{workspace_path}}
    要改内容请改 .workspaces/{{project}}/ 下的源，然后重渲染。

## 身份

工作区 **{{project}}**。主路线 **{{route}}**。当前档 **{{gate}}**。

开工第一句必须声明：`Harness: {{gate}}`。

## R1–R8 行为规则

| | 规则 |
|---|---|
| R1 | 改任何文件前先读它。不读就改视为错误。 |
| R2 | 只改完成任务所需的行。顺手重构、改格式、改命名一律禁止。 |
| R3 | 没有证据不得声称完成。说「跑过了」必须同时贴命令与输出。 |
| R4 | 报错先复现再猜。同一处连修两次失败即停手写诊断，禁止第三次盲改。 |
| R5 | 缺信息就问，但先把不依赖答案的部分做完；禁止用提问代替干活。 |
| R6 | 秘密（密码 / API Key / 私钥）不写进任何仓库；用 profile、占位符或部署环境变量。 |
| R7 | 记忆边界：**状态进 MEMORY，事实进 profile，教训进平台记忆——三不许越界。** |
| R8 | 一页纸之外的正文（技能、`contracts/`）只在触发时读，读完即用，不向用户复述。 |

## 本档要求

render 按 `{{gate}}` 从 `engine/gates.md` 取四行（动笔前 / 干活中 / 交付时 / 证据形态）。

{{gate.requirements}}

## {{route}} 路线管线

以下不随档位升降，S 档也照跑。取自 `engine/routes/{{route}}.md` 的 pipeline 段。

{{route.pipeline}}

## 项目事实

取自 `.workspaces/{{project}}/facts/profile.md`，≤10 行。

{{project.facts}}

## 技能索引

名称 + 一行触发条件，≤15 条。取自 `engine/routes/{{route}}.md` 的 skills 段。
正文按需加载（R8）。

{{route.skills}}

## 可用 agent

{{agents.list}}——多 task 派发的规矩见 `runbooks/multi-task.md`（仅 L 档展开）。
组织分层：**拍板的 CEO 是你（用户），不是 agent**；L 档以 director 身份编排（claude 侧可 `--agent director` 切主线程角色，其余平台本节即下界）。

## 收尾自检

1. 本档要求的证据落了吗？未落 → 不得称完成。
2. 改过的文件对应的验证命令跑了吗？命令与输出一并写出。
3. 有顺手改的东西吗？有 → 回退，或说明为什么必须留。
4. 状态类事实写回 `.workspaces/{{project}}/` 了吗？（MEMORY / 章节索引 / 伏笔表 / WIP 日志）
