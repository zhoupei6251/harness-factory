# .harness/state/config.md — 项目元数据（人类 seed）
# 改完跑 npm run harness 重渲染
route: code            # code | novel | news
project_name: ""       # 项目根 basename（自动检测）
engine_path: "../harness-factory"   # 引擎相对路径
