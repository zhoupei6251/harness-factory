{
  "name": "harness-factory",
  "version": "0.1.0",
  "description": "Clean skeleton for shipping harness rules to 4 AI platforms",
  "private": true,
  "type": "module",
  "scripts": {
    "bootstrap": "tsx scripts/bootstrap.ts",
    "validate": "tsx tests/validate-schemas.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
